# ESTADO — nook-tools

> Fuente única de verdad de este repo.
> Se actualiza en el mismo commit que el cambio, nunca aparte.
> Regla: si no se puede verificar, se escribe "SIN VERIFICAR", no se inventa.

**Última actualización:** 2026-09-18 (Site URL de Authentication corregida de `localhost:3000` a la URL real de producción -- bloqueaba login/confirmación de usuarios reales, reportado por Roberto Calixto. Ver bloqueador #7 en sección 7)
**Actualizado por:** Cla (verificación con `generate_link` real contra producción). Historial previo: agente retomando sesión interrumpida (mismo encargo, sin repetir trabajo ya hecho), a petición de Mario
**Cliente:** NOOK World Cuisine (VOCO Surfside Aruba)

> Nota: un intento anterior de interconectar las 4 herramientas se cayó por un error
> de conexión a mitad de las pruebas, sin dejar nada a medias comiteado (el repo
> siguió en `9e2b984`). Esta entrada documenta el intento que sí se completó.

---

## 1. Qué es esto

Hospedaje estático (GitHub Pages, repo público, gratis) para herramientas internas de
NOOK. Tiene "Fichas de Platillos" (`fichas/`) y ahora "Recipe Master Book" (`recetario/`);
Mario planea subir más herramientas aquí mismo.

## 2. Dónde vive

| Recurso | Valor | Verificado |
|---|---|---|
| Repo | `dmzkitchensupport/nook-tools` | ✅ público, creado 13 sep 2026 |
| Producción (URL) | `https://dmzkitchensupport.github.io/nook-tools/` | ✅ (ver bloqueador #1 — confirmar tras el primer deploy) |
| Backend | Las 6 herramientas comparten el mismo proyecto Supabase `nook-tools` (ref `fcswffoilwqltkghlvff`, org `osoiyzwcumyucmvjbkmi`), separado de Cockpit a propósito — ahora vía login real (`auth.users`+`profiles`) en vez de la contraseña compartida vieja. Solo `bitacora/` guarda datos propios en tabla (`bitacora_entries`); las otras 5 solo usan el login/gate, sus datos siguen en `localStorage`/IndexedDB del navegador. | ✅ verificado con RPC reales (anon key + sesión) contra la API en producción — ver entrada 2026-09-18 |
| CI / Actions | Ninguno todavía (regla 10 SÍ empieza a aplicar ahora que `bitacora/` maneja ventas reales — pendiente evaluar) | ⚠️ pendiente |

## 3. Historial de cambios reales

| Fecha | Qué cambió | Commit |
|---|---|---|
| 2026-09-18 | **Login real (Supabase Auth) reemplaza la contraseña compartida de `bitacora/` en las 6 herramientas + panel de administración nuevo.** Sesión retomada tras una interrupción — el trabajo de código y la migración SQL ya estaban hechos por el intento anterior; esta sesión los verificó de fondo, cerró huecos y comiteó/publicó. Esquema: tabla `public.profiles` (email/nombre/teléfono/puesto/rol/aprobado), trigger `on_auth_user_created` que da de alta automáticamente a `rol='pendiente', aprobado=false` al registrarse, RPC `admin_list_profiles`/`admin_set_profile` (`SECURITY DEFINER`) que **solo aceptan `auth.email() = 'mario@delamorazumaran.com'`** — verificado en producción real: una cuenta con `rol='gerencia'` ya aprobada recibe `"No autorizado"` al llamar ambas RPC, ni siquiera puede leer la lista de cuentas. `bitacora_save/list/delete` ya no piden contraseña — exigen sesión real con `rol='gerencia' AND aprobado=true` vía función `require_gerencia()` (verificado: cuenta `chef` rechazada con `"acceso denegado"` al llamar `bitacora_list`). RLS de `profiles`: cada quien solo lee su propia fila (verificado: una cuenta `chef` no puede leer la fila de otra cuenta vía REST). Sin grant de update sobre `rol`/`aprobado` a `authenticated` — nadie se autoaprueba ni autoasigna rol aunque intente manipular el request directo. `assets/auth.js` (nuevo, compartido por las 6 herramientas + `index.html`/`cocina/`/`formatos/`/`admin/`) trae el overlay de login/registro, pantalla de "cuenta pendiente" (con atajo al panel de admin si detecta que quien inició sesión es Mario) y pantalla de "acceso denegado" por rol. `admin/index.html` (nuevo) lista todas las cuentas y permite editar puesto/rol/aprobado por fila, llamando a `admin_set_profile`. Reparto de acceso por rol confirmado en cada archivo: `chef` y `gerencia` entran a `cocina/`, `fichas/`, `formatos/`, `organizador/`, `generador/`, `recetario/`; solo `gerencia` entra a `bitacora/` y `salon-barra/` (probado por URL directa, no solo por el menú — una cuenta `chef` fue rechazada en ambas al navegar directo). El menú principal (`index.html`) además oculta/atenúa visualmente las tarjetas de Bitácora y Salón y Barra cuando quien entra es `chef`. La migración `supabase/migrations/20260918000000_profiles_and_auth_gate.sql` ya estaba aplicada de verdad al proyecto Supabase `nook-tools` (ref `fcswffoilwqltkghlvff`) desde el intento anterior — confirmado de nuevo en esta sesión contra la base real (`supabase db query --linked`): las 9 funciones del esquema existen, la tabla `bitacora_config` y la función `bitacora_check_password` (mecanismo viejo) ya no existen, `bitacora_entries` sigue intacta (0 filas, no había datos reales todavía). Nunca se subió ninguna service-role key al repo — toda la verificación de RPC/RLS se hizo con el anon key público + sesiones reales de prueba. **Verificación real ejecutada, no solo supuesta:** `node --check` sobre los 16 bloques de script embebidos de las 10 páginas tocadas + `assets/auth.js` (0 errores); Playwright real contra el servidor local sirviendo estos mismos archivos ya modificados, apuntando al Supabase de producción: cuenta pendiente sin acceso a `fichas/`/`bitacora/`/`cocina/`/`salon-barra/`/`formatos/`; login de administración con una cuenta real creada para pruebas en `mario@delamorazumaran.com` (ver bloqueador de contraseña abajo), tabla del panel cargó y `admin_set_profile` aprobó una cuenta nueva registrada por el flujo real de signup (confirmado también contra la base, no solo la UI); cuenta `chef` entró OK a `fichas/` y fue rechazada en `bitacora/` y `salon-barra/` por URL directa; cuenta `gerencia` entró OK a ambas. Capturas de pantalla reales guardadas de: login, panel de admin (antes/después de aprobar), pantalla de "cuenta pendiente" y pantalla de "acceso denegado". Las 3 cuentas de prueba genéricas (`qa.nook.chef.demo@gmail.com`, `qa.nook.gerencia.demo@gmail.com`, `qa.nook.pendiente.demo@gmail.com`) se borraron de la base real al terminar, para no dejar basura en el panel de administración que Mario va a ver de verdad. Se agregó `.gitignore` (no existía) para excluir `supabase/.temp/` (caché local del CLI, no es código fuente). | *(este commit)* |
| 2026-09-13 | Repo creado, primera herramienta cargada: `fichas/index.html` (Fichas de Platillos — captura foto+platillo+insumos+casilla "no disponible", autoguardado en localStorage). Corregido antes de subir un bug real de sintaxis que traía el archivo original (línea corrupta que rompía todo el script, ver detalle en la sesión) y el bug de un solo argumento en `downloadUpdatedCopy()`. Verificado con `node --check` + render real en Chrome headless (6 fichas cargando, checkbox presente) antes de commitear. | *(commit anterior)* |
| 2026-09-13 | Segunda herramienta: `recetario/index.html` — "NOOK Recipe Master Book" (83 recetas + 56 sub-recetas, tal como fue recibido por WhatsApp). Se le agregó: (1) Modo edición (✏️) — nombre/cantidad/unidad de cada ingrediente y cada paso de procedimiento se vuelven editables in situ; (2) autoguardado en `localStorage` con el mismo patrón `queueSave`/`setStat` de `fichas/`; (3) Registro de cambios (📋) — panel con receta, campo, valor anterior → nuevo y fecha/hora real de cada edición, persistente; (4) las 141+1 menciones de "April 2026"/"APRIL 2026" del header y pies de tarjeta se corrigieron a la fecha real (13 sep 2026) — las recetas editadas llevan además su propio sello "· editado <fecha real>"; (5) 3 botones de conversión global de unidades [ONZAS][GRAMOS][LIBRAS] — convierten las cantidades de las 83+56 recetas a la vez, siempre recalculando desde el valor original (nunca desde el valor ya convertido, para no acumular error de redondeo), con `1 oz = 28.3495 g` y `1 lb = 453.592 g`; unidades no convertibles (`pcs`, `pinch`, `to coat molds`) se dejan intactas en los 3 modos; el modo elegido se recuerda en `localStorage`. El buscador que ya traía el archivo no se tocó y sigue funcionando igual. | *(commit anterior)* |
| 2026-09-13 | **Interconexión de las 4 herramientas NOOK** (esquema JSON universal, documentado en `README.md`). (1) `catalogo-canonico.json` — 83 recetas extraídas de `recetario/index.html` (nombre, familia, código `R-##`, insumos; precio/descripción vacíos a propósito, el recetario no los trae — ver notas reales en `README.md`, incluye 2 correcciones de un bug propio de extracción: R-83 arrastraba de más los insumos de las 56 sub-recetas por un split de HTML mal delimitado, y R-44 perdía sus 9 insumos por un atributo `style` extra en la celda — ambos corregidos y verificados antes de generar el archivo final). (2) `fichas/index.html` — su `CATALOG` interno ahora se reconstruye desde `catalogo-canonico.json` (antes tenía ~88 filas con precios/descripciones de la carta real, capturadas en una auditoría previa — **se dejaron de usar a propósito**, mezclarlas a ciegas con las 83 recetas del recetario habría requerido reconciliar nombres que no coinciden 1:1 entre code R-## y nombre de carta, ver "Pendiente" abajo); se agregó botón "Importar JSON" (matchea por `id` de foto) y se corrigió `exportJSON()` para usar las claves exactas del esquema universal (`carta`→`familia`, `no_disponible`→`disponible`, invirtiendo la polaridad). El `data` real ya capturado por Mario (61 fotos con platillo/descripción/insumos propios) **no se tocó**. (3) `organizador/index.html` (nuevo, antes `NOOK_Editor_Completo.html` en el Desktop de Mario) — botones reales "Exportar JSON"/"Importar JSON" agregados sobre el `DISHES`/`state` existente; el import matchea por `id` de foto y normaliza familias entrantes (`LUNCH`/`BREAKFAST`→`LUN`). (4) `generador/index.html` (nuevo, antes el archivo de "NOOK RECIPE 2026" en el Desktop de Mario) — botones "Exportar universal"/"Importar universal" agregados sin tocar su `.json` de proyecto propio (con fotos) ni el `SEED` embebido; export sin fotos, import matchea por nombre de platillo normalizado (este archivo no tiene un namespace de `id` de foto compartido con los otros 3). Los 4 archivos finales pasaron `node --check` (0 errores) sobre su script embebido. | *(este commit)* |

| 2026-09-17 | **Fix real: `generador/index.html` no dejaba editar el campo "Procedure" en fichas nuevas/recién importadas (reportado por Mario con captura de pantalla).** Diagnóstico verificado con Playwright real (no supuesto): en Chromium el campo funcionaba; en WebKit (motor de Safari/iPad — el entorno probable de uso en cocina) el campo recibía el foco al tocarlo pero el texto tecleado no se insertaba nunca. Causa raíz aislada con pruebas controladas: WebKit no acepta entrada de teclado en un elemento `contenteditable` vacío cuyo CSS computado es `display:inline` (afecta por igual a `<span>` o `<div>`, no es un problema de la etiqueta). El propio archivo ya tenía la solución aplicada en otro campo (`.f-meta .ed{display:inline-block}` para Portion/Time/Rush) pero no se había aplicado al campo Procedure. Fix de una línea: agregada `.f-proc .ed{display:inline-block; min-width:1.2ch}` (mismo patrón). Verificado antes de subir: `node --check` sobre el script embebido (0 errores), prueba real de tap+escritura en WebKit/iPad (ahora sí guarda el texto) y en Chromium (sin regresión), y captura de pantalla de una ficha para confirmar que el layout visual no se rompió. | *(este commit)* |

| 2026-09-17 | **Reordenamiento del flujo de trabajo a 3 pasos numerados**, a petición explícita de Mario (confirmado con él: el Recetario queda fuera de la secuencia, como referencia). `index.html` (portal) ahora muestra Paso 1 → Organizador de Fotos, Paso 2 → Fichas de Platillos, Paso 3 → Generador de Ficheros, con el Recipe Master Book en una sección aparte de "Referencia". `README.md` documenta la misma lógica: el Generador es el producto final y su esquema JSON universal es el que las otras 2 herramientas del flujo deben poder hablar; el Recetario es fuente de insumos/procedimiento, ya completo, no se rehace por cada foto. Verificado visualmente con captura de pantalla antes de subir. | *(este commit)* |

| 2026-09-17 | **Menú principal + reestructura del sitio**, a petición de Mario, quien compartió `Nook_Brandbook.pdf` (66 páginas) como referencia de diseño. Se creó `index.html` (raíz) como menú principal con 4 secciones: Cocina, Formatos, Bitácora, Salón y Barra — íconos propios dibujados a mano en SVG (fork+knife, clipboard, libro abierto, copa; sin emojis, por instrucción explícita), logo real recortado de la página "Nuestra firma" del brandbook (`assets/nook-logo-navy.png`, navy sobre transparente, verificado visualmente antes de usar) y paleta oficial del manual (navy `#2b4a5e`, tostado `#9a7367`/`#b8935a`, crema `#faf9f6` — confirmados contra la sección "07 Colores" del PDF, hex `#2f443d` y `#9a7367`). La tipografía de marca (Isidora Sans) no se pudo aplicar — es una fuente con licencia, no se tiene el archivo; se dejó el stack de sistema, señalado como pendiente si Mario consigue la licencia/archivo. `cocina/` contiene el flujo de 3 pasos (antes en la raíz) — los archivos de `fichas/`, `recetario/`, `organizador/`, `generador/` **no se movieron de la raíz** para no romper bookmarks ni rutas; `cocina/index.html` solo enlaza a ellos. `formatos/`, `bitacora/`, `salon-barra/` son placeholders "Próximamente" — sin contenido inventado. Verificado con captura de pantalla del menú principal y de `cocina/` antes de subir. | *(este commit)* |

| 2026-09-17 | **Tipografía en las páginas nuevas del menú.** Isidora Sans (la fuente real de la marca) es de licencia paga — no se tiene el archivo. Mario confirmó "una similar, da igual" — se aplicó **Poppins** (Google Fonts, gratuita) al menú principal, `cocina/` y los 3 placeholders, por similitud geométrica real con el specimen del brandbook (contraste de trazo uniforme, "o" perfectamente circular). Las 4 herramientas ya en producción (`fichas/`, `recetario/`, `organizador/`, `generador/`) no se tocaron — Mario pidió "lo más sencillo sin romper nada". Verificado con captura de pantalla antes de subir. | *(este commit)* |

| 2026-09-17 | **Pulido visual del menú (index.html, cocina/, formatos/, bitacora/, salon-barra/)** — a petición de Mario ("mejora el diseño sin romper nada"). Se creó `assets/nook.css` (hoja compartida, antes cada página repetía su propio `<style>`) con: patrón de puntos sutil de fondo (`radial-gradient`, tono tostado al 10% de opacidad — mismo lenguaje que la sección "Patterns" del brandbook), íconos con fondo circular de color (uno de los 4 tonos de marca por sección: navy/tostado/verde-navy profundo/marrón), conector visual entre los 3 pasos de `cocina/`, tagline "WORLD CUISINE" bajo el logo real, y los 3 placeholders con ícono grande + pastilla "Próximamente" en vez de la caja punteada. **Las 4 herramientas de producción (`fichas/`, `recetario/`, `organizador/`, `generador/`) no se tocaron** — verificado con `git diff --stat` (0 cambios) antes de subir, y con capturas de pantalla de las 5 páginas modificadas. | *(este commit)* |

| 2026-09-17 | **Rediseño con color real** — el pulido anterior (fondo de puntos sutil, tarjetas blancas) le pareció a Mario "una página normal, sin vida" y pidió más color. Se reemplazó por bloques de color sólido tomados de la paleta oficial en las 5 páginas del menú: franja superior (hero) de color por sección, tarjetas del menú principal 100% a color (no blancas) cada una con su propio patrón decorativo tomado 1:1 de la sección "Patterns" del brandbook — rayos (Cocina, navy), puntos (Formatos, tostado), ondas (Bitácora, verde-navy profundo), diagonales (Salón y Barra, marrón). Se generó `assets/nook-logo-cream.png` (versión clara del logo, recoloreada a partir del navy transparente) para usar sobre los fondos oscuros del hero. Las 4 herramientas de producción siguen sin tocarse — verificado con `git diff --stat` (0 cambios) antes de subir. | *(este commit)* |

| 2026-09-17 | **Bug real de caché (no de diseño), encontrado con evidencia — Mario mandó captura mostrando el menú sin ningún color.** Verificado con `curl` + `shasum` que el servidor SÍ tenía el CSS correcto (hash idéntico al archivo subido); el problema es que GitHub Pages manda `Cache-Control: max-age=600` en `assets/nook.css`, así que el navegador de Mario se quedó con la hoja de estilos vieja (blanca) mientras el HTML nuevo sí cargó — de ahí que se vieran elementos nuevos (la onda de Bitácora) sin ningún color de fondo. Corregido agregando `?v=3` al link del CSS en las 5 páginas del menú, para forzar que el navegador pida el archivo nuevo cada vez que se suba una versión distinta (subir el número la próxima vez que `nook.css` cambie). | *(este commit)* |

| 2026-09-17 | **Segunda pasada de diseño, esta vez bajo el estándar del subagente `disenio-creativo`** (Mario recordó que sí existe — vive dentro del dominio de `vision-master`, definido en `dmz-ecosystem`; no es invocable por nombre desde este repo, así que esta sesión adoptó su brief/mandatos directo: "cero plantillas genéricas", RACREEA, coherencia de Director Creativo). Se reemplazó el grid parejo de 4 tarjetas iguales por una jerarquía real: **Cocina** (lo único ya construido) pasa a tarjeta destacada de ancho completo con chips de los 3 pasos; **Formatos/Bitácora/Salón y Barra** (vacíos) bajan a una fila secundaria más discreta con etiqueta "Próximamente" visible en la propia tarjeta. Se agregó un divisor de borde orgánico (SVG ondulado) entre el hero y el contenido en las 5 páginas, en vez del corte recto anterior. Bug real encontrado y corregido en el camino: la tarjeta destacada heredaba `flex-direction:column` de la regla genérica `.card` por no declararlo explícito en `.featured` — verificado visualmente antes/después con captura de pantalla real (Chromium). CSS versionado a `?v=4`. | *(este commit)* |

| 2026-09-17 | **Quinta herramienta real: `bitacora/index.html` — Bitácora Diaria A&B**, construida a partir de 3 documentos que Mario compartió por WhatsApp (el PDF de la Bitácora Diaria actual, y dos Excel/capturas que Roberto lleva a mano: "Ventas y Proyección Diaria" y "Comparativo Desayunos/Ocupación"). Todos los campos del PDF original están replicados 1:1 (Área General, Cocina, Caja, Cheque Promedio, Gerencia, Barra, Venta por Áreas, Totales). Se agregaron 5 campos que el PDF no traía (Guest in House, Incl., No Show, Pay, Locales) — necesarios para que el corte de ocupación se genere solo, en vez de que Roberto lo seguya llevando aparte en Excel. Autoguardado en `localStorage` (mismo patrón que las otras 4 herramientas), pestaña de Historial (abrir/borrar cualquier día, exportar/importar JSON de respaldo) y pestaña de Cortes (ventas+proyección y ocupación, ambos recalculados solos por mes a partir de lo capturado — no hay que llevar el Excel aparte). "Ventas totales" se auto-calcula (suma de las 5 áreas) en vez de capturarse a mano. **Fórmula del % de ocupación marcada explícitamente como SIN VERIFICAR** en la UI (Total Guest = Incl−NoShow+Pay, %=TotalGuest/GuestInHouse) — al probarla contra el ejemplo real de Roberto (día 1 de sept) no coincidió exactamente (dio 30% vs. 36% en su Excel), así que no se debe usar como reporte oficial hasta que él confirme el criterio real. **PDF real (no solo impresión):** se generó con jsPDF (CDN, sin costo) replicando el diseño de color por sección del PDF original. **Envío a WhatsApp:** decisión explícita de Mario — un toque vía Web Share API (abre el panel nativo del teléfono con el PDF ya adjunto, Roberto elige WhatsApp); NO se conectó al puente de WhatsApp personal (Baileys) para automatización 100% sin toques — eso requeriría decidir su hosting 24/7 y un servidor intermedio, queda como posible upgrade futuro si Mario lo pide. Verificado con Playwright real: cálculo de ventas totales, historial, ambos cortes y generación de PDF (8.4KB, sin errores de consola) antes de subir. Las 4 herramientas de producción no se tocaron. | *(este commit)* |

| 2026-09-17 | **Bug de la fórmula de % de ocupación corregido y verificado contra las 17 filas reales del Excel de Roberto (1-17 sept), no solo el ejemplo del 16.** El % dividía "Total Guest" entre "Guest in House" — el Excel real divide "Total" (Total Guest + Locales) entre "Guest in House". Con el divisor correcto, las 17 filas de septiembre coinciden exacto (probado con Playwright, 17/17 OK); la nota "SIN VERIFICAR" se quitó de la UI porque ya está confirmado. | *(este commit)* |

| 2026-09-17 | **Sexta herramienta real: `salon-barra/index.html` — Generador de Ficheros para bebidas**, a petición de Mario ("plantilla en blanco del mismo formato de ficha, sin inventar ni modificar estructura" — confirmado tras una pregunta de aclaración, ya que dos prompts previos ambiguos sin ejemplo adjunto apuntaban a lo mismo). Es una copia exacta de `generador/index.html` con 4 cosas vaciadas: `SEED` (fichas reales de cocina), `MIG_PROC`/`MIG_ING` (procedimientos/insumos reales), y `MENU` (el menú oficial de comida transcrito — su propio comentario en el código decía "sin bebidas", no correspondía dejarlo en una herramienta de bebidas). Lo que NO se tocó, por instrucción explícita de preservar la estructura verbatim: las 4 categorías de servicio (Breakfast/Lunch/Pool·Bar/Dinner) y toda la mecánica de edición/exportación — si esa taxonomía no encaja con bebidas reales, queda como decisión de negocio pendiente de Mario, no se inventó una nueva. Se agregó link "← Menú principal" en el header (el original `generador/` no lo tiene, por diseño, ya que no vivía bajo el menú nuevo). Verificado con Playwright real: arranca en 0 ficheros, "+ Nueva ficha" y edición de texto funcionan, el botón Índice muestra 0/0 en las 4 categorías sin ningún platillo real filtrado, 0 errores de consola. Las 4 herramientas de producción de cocina no se tocaron (`git diff --stat` limpio). | *(este commit)* |

| 2026-09-17 | **Bug real de colisión de datos entre `generador/` y `salon-barra/`, encontrado al investigar la pregunta de Mario "¿dónde se guarda todo esto, hay backup?".** Ambas herramientas usaban el mismo nombre de base IndexedDB (`nook_ficheros`), mismo store (`proj`) y misma clave (`'project'`) — como IndexedDB se comparte por origen (no por ruta) y las dos viven en `dmzkitchensupport.github.io`, si alguien abría las dos en el mismo navegador, una podía pisar los datos de la otra. **Reproducido en producción real antes de arreglarlo:** se creó una ficha de prueba en `generador/` y al abrir `salon-barra/` la arrastraba. Corregido cambiando el nombre de la base de `salon-barra/` a `nook_ficheros_salonbarra` (aislada). Verificado de nuevo contra producción tras el fix: ya no colisionan. | *(este commit)* |

| 2026-09-17 | **`organizador/index.html` no tenía NINGÚN autoguardado** (a diferencia de las otras 5 herramientas) — hallazgo real al investigar la pregunta de Mario sobre dónde vive la información. El estado vivía solo en memoria del navegador; cerrar la pestaña sin exportar JSON perdía todo. Agregado autoguardado en `localStorage` (clave `nook:organizador:v1`), mismo patrón que `fichas/`/`recetario/`/`bitácora/` — enganchado en `render()` (el único punto por el que pasan reordenar/resetear/importar) y también en el listener de renombrar (que muta `state` sin pasar por `render()`, para no perder el foco del input en cada tecla). Verificado con Playwright real: escribir un nombre, recargar la página, el nombre sigue ahí. | *(este commit)* |

| 2026-09-17 | **Respaldo real en la nube para `bitacora/`** — a petición de Mario ("sí" a la recomendación, tras preguntar dónde vive la información del sitio y confirmar que no había ningún backup automático). Se creó un proyecto Supabase nuevo y separado (`nook-tools`, ref `fcswffoilwqltkghlvff`) — **no se reutilizó el Supabase de Cockpit** aunque ya tiene un tenant NOOK real: Cockpit es otro producto (costeo/menu engineering, con login+RLS por rol), mezclarlo habría expuesto su base a un anon key de un sitio público sin login. Tabla `bitacora_entries` sin ninguna policy para `anon` — todo pasa por 3 funciones RPC (`bitacora_save`/`bitacora_list`/`bitacora_delete`, `SECURITY DEFINER`) que verifican una **contraseña compartida** contra un hash bcrypt (`pgcrypto`) antes de tocar la tabla; la contraseña real no vive en ningún archivo de este repo (público) — se comunicó a Mario aparte, fuera de git. `bitacora/index.html` ahora arranca con una pantalla de contraseña (guardada en `localStorage` tras la primera vez); al desbloquear, trae lo que haya en la nube y lo fusiona con lo local; al guardar, sincroniza a ambos. Verificado con Playwright simulando 2 dispositivos reales: uno guarda, el otro (navegador nuevo, contraseña nunca antes puesta) sí ve la ficha guardada por el primero — resuelve exactamente el síntoma que reportó Mario ("la persona que edita ve los cambios, la que abre el link no"). Bug real encontrado y corregido en el camino: el overlay de contraseña no se ocultaba tras desbloquear (regla CSS incompleta). Migración SQL versionada en `supabase/migrations/` **sin la contraseña real** (se redactó antes de comitear — el archivo original de trabajo sí la traía en texto plano porque el hash se calcula al aplicar la migración). Las otras 5 herramientas no se tocaron. | *(este commit)* |

| 2026-09-18 | **Bug real de zona horaria en `bitacora/` — encontrado al confirmar Mario que Eduardo y Yasmil operan desde Aruba (AST, UTC-4).** Las 4 fechas que calcula el archivo (fecha por defecto del formulario, nombre del respaldo JSON, mes por defecto de Cortes) usaban `new Date().toISOString().slice(0,10)`, que calcula en UTC, no en la hora local del dispositivo. Reproducido con un caso real: alguien capturando a las 11:30pm hora Aruba el 18-sep quedaba con `2026-09-19` en el campo de fecha — el corte de cierre de turno se habría guardado en el día equivocado. Corregido con una función `hoyLocal()` (`toLocaleDateString('en-CA')`, respeta la hora del dispositivo) y reemplazados los 4 usos. Revisado también el campo Teléfono del registro (`assets/auth.js`) — es texto libre sin formato forzado, un número de Aruba (+297) no se rechaza, no requirió cambio. Las otras 5 herramientas no usan cálculo de fecha por UTC, no les aplica este bug. Verificado con `node --check` y con el caso reproducido arriba. | *(este commit)* |

## 4. Bloqueadores

- [x] **#5 — RESUELTO 18 sep 2026 (incidente de gobernanza, ver nota abajo):** para poder verificar el panel de admin, una sesión anterior creó la cuenta real `mario@delamorazumaran.com` directo en Supabase Auth con una contraseña temporal de prueba y **la dejó en texto plano en este mismo archivo, comiteada y publicada en el repo público** — violación directa de la regla de este repo ("Contenido público — sin datos sensibles", `CLAUDE.md`) y ahora regla 12 de `GOBERNANZA.md`. Contención real: Mario borró esa cuenta desde Supabase Dashboard (Authentication → Users → Delete user) y se registró de nuevo él mismo por el flujo normal de signup con una contraseña que nunca compartió con el agente; el panel admin lo deja entrar por coincidencia de correo aunque su propio perfil estuviera `pendiente`, así que se autoaprobó como `gerencia` — verificado con query real a `profiles` (`rol: gerencia, aprobado: true`). El commit que exponía la contraseña vieja se reescribió (`git commit --amend`) y se hizo `force-push` para quitarla de `origin/main` (con la salvedad honesta de que GitHub puede retener el commit huérfano accesible por hash hasta recolectarlo). Registrado como incidente formal de gobernanza — **regla 12 agregada a `dmzkitchensupport/dmz-estado/GOBERNANZA.md`** y propagada al resumen de este `CLAUDE.md`, atribuido al Master Agent Tech.
- [ ] **#6 — Eduardo y Yasmil no tienen cuenta creada todavía (no se inventó ninguna):** el encargo original los asigna a `rol='gerencia'`, pero ninguno de los dos se ha registrado — no existen en `auth.users` ni en `profiles`. Falta que cada uno entre a cualquier herramienta y se registre con su propio correo/contraseña real; después Mario los aprueba desde `admin/` asignándoles `gerencia`. Nadie puede hacer esto por ellos.
- [x] **#7 — RESUELTO 18 sep 2026: SMTP propio configurado con Resend.** Mario ya tenía cuenta Resend con dominio verificado `litasupport.com` (usada en otros proyectos DMZ); generó una API key nueva con permiso solo de envío (`nook-tools-smtp`) y la capturó él mismo en Supabase → Authentication → Emails → SMTP Settings (`smtp.resend.com:465`, usuario `resend`, remitente `nook@litasupport.com` / "NOOK Tools"). Verificado real: el registro de `mario@delamorazumaran.com` mandó el correo de confirmación de verdad (antes daba `429 - email rate limit exceeded`), el clic lo confirmó server-side (`email_confirmed_at` real en `auth.users`, verificado con query directa) y pudo iniciar sesión. **RESUELTO 18-sep-2026 (mismo día, más tarde):** lo que se documentó arriba como "cosmético, no bloqueante" en realidad SÍ bloqueaba — Roberto Calixto reportó por WhatsApp `ERR_CONNECTION_REFUSED` en `localhost` al intentar entrar, confirmando que el rebote rompía el flujo real para un usuario real, no solo se veía raro. Mario cambió "Site URL" a `https://dmzkitchensupport.github.io/nook-tools/` y agregó `https://dmzkitchensupport.github.io/nook-tools/**` a Redirect URLs, directo en el dashboard (Cla no lo hizo por CLI a propósito: `supabase config push` sube la configuración completa de Authentication y no había forma de leer primero los valores del SMTP recién configurado — riesgo real de borrarlo sin querer). **Verificado contra producción real, no solo confiando en la captura de pantalla:** `POST /auth/v1/admin/generate_link` (tipo `recovery`, con el service_role key real del proyecto) devolvió `redirect_to: https://dmzkitchensupport.github.io/nook-tools/` — ya no `localhost:3000`.
- [x] **#1 — RESUELTO 13 sep 2026:** verificado con `curl` real — `/` y `/fichas/` responden HTTP 200, contenido correcto (`<h1>NOOK — Herramientas</h1>` y `<title>NOOK · Fichas de Platillos</title>`), build de Pages en estado `built` sin error (`gh api .../pages/builds/latest`).
- [x] **#2 — Contenido público a propósito, sin dato sensible (revisado de nuevo con `recetario/`):** el Recipe Master Book trae nombre/familia/ingredientes/cantidades/unidades y procedimiento — no trae precios de venta, costos, ni credenciales. Igual de público-apropiado que `fichas/`.
- [ ] **#3 — Auditoría de datos de negocio (NO corregida por el agente, requiere decisión de Mario):** al cotejar este Recipe Master Book contra Cockpit (`recetas_maestras` tenant NOOK), se encontró que el código **R-43** tiene nombres distintos en cada sistema — este libro dice "TORTA BASCA (150G PORTION)" (y los insumos de Cockpit para R-43 — TARTA BASCA PORTION, COOKIE CRUMBLE, BERRY SAUCE — coinciden con esa receta, no con un platillo separado); Cockpit tiene guardado el nombre "COOKIE CRUMBLE" para R-43. Todo indica que el nombre en Cockpit está mal cargado (se usó el nombre de un insumo como nombre del platillo), pero no se corrigió — es dato de negocio en producción y la regla de gobernanza pide que lo decida Mario, no el agente. Además: **R-44 BROWNIE A LA MODE** (9 insumos reales, en gramos) y **R-82/R-83** (marcados en el propio libro como "RECETA POR DEFINIR — CHEF MARIO") no tienen insumos cargados en Cockpit — R-82/R-83 es consistente (el libro mismo dice que faltan), pero R-44 sí tiene receta completa aquí y cero costeo en Cockpit.
- [ ] **#4 — `fichas/` perdió precio/descripción de carta al cambiar de fuente (requiere decisión de Mario):** su `CATALOG` viejo (~88 filas) traía precio y descripción reales, capturados en una auditoría previa de las cartas impresas. El nuevo `CATALOG` (generado desde `catalogo-canonico.json`, ver README) no los trae porque el recetario es un libro de cocina, no la carta — y fusionar ambas fuentes a ciegas habría requerido resolver por código de receta nombres que no coinciden 1:1 (mismo caso que R-01/R-12/R-30 documentado en `NOOK_Auditoria_Catalogo_Base_2026-09-13.md` del Desktop de Mario). Los datos viejos siguen recuperables en el historial de git (commit `9e2b984`, antes de este cambio) por si Mario quiere reconciliarlos receta por receta más adelante.

## 5. Qué sigue

Inmediato: Mario cambia su contraseña temporal (bloqueador #5), y Eduardo/Yasmil se
registran para que Mario los apruebe como `gerencia` desde `admin/` (bloqueador #6).
Evaluar si conviene SMTP propio para registros (bloqueador #7) si empieza a estorbar en
la práctica. Mario subirá más herramientas NOOK a este mismo repo (cada una en su propia
subcarpeta, con su propio `index.html`, enlazada desde la portada raíz — recordar
agregar el guard de `assets/auth.js` a cada una nueva). Pendiente que Mario resuelva
el bloqueador #3 antes de que Cockpit se use como fuente de costeo definitiva de R-43/R-44,
y el #4 si quiere recuperar precio/descripción de carta en `fichas/`.
